import React from "react";
import styled from "styled-components";

const Background = () => {
  return <CanvasContainer id="canvas"></CanvasContainer>;
};

const CanvasContainer = styled.canvas``;
export default Background;
